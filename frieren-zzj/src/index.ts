/**
 * Frieren × Himmel web theme, node half.
 *
 * Deliberately empty: the theme is a browser-surface feature — token
 * overrides, a global stylesheet, and decorative slot entries. The node half
 * exists so the `dsh.client` row is dual-face like every other roster entry.
 */

/** Host plugin body — the browser half carries the whole theme. */
export function apply(): void {}
